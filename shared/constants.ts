// Норма рабочего дня в часах. Используется как дефолт: у каждого юзера своё
// значение в User.workHours, задаётся пригласившим при инвайте.
export const WORK_DAY_HOURS = 8;

/** Сколько дней живёт ссылка из письма-приглашения. */
export const INVITE_TTL_DAYS = 7;

/**
 * Потолок вложений на одну задачу. Суммарные байты и так режет квота тарифа
 * (assertStorageQuota), но без счётчика одну задачу можно раздуть десятками
 * картинок — список и документ пухнут. Держим число обозримым.
 */
export const MAX_ATTACHMENTS_PER_TASK = 10;
